import React from 'react';

interface IMainProps {

}

const Main: React.FC<IMainProps> = (props: IMainProps) => {

    return <>
        <header></header>
        <main>
            <div>
            </div>
        </main>
        <footer></footer>
    </>
}

export default Main;
