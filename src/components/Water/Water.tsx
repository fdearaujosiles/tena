import styles from './Water.module.scss';
import React from 'react';

interface IWaterProps {}

const Water: React.FC<IWaterProps> = (props: IWaterProps) => {

    return <div className={styles.Water} >
    </div>
}

export default Water;
